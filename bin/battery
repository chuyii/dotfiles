#!/usr/bin/perl

$battery = `upower -i \`upower -e | grep battery\``;
$percentage = `upower -i \`upower -e | grep battery\` | grep percentage`;

$percentage =~ /(\d+%)/;
$percentage = $1;

print "$percentage\n";
