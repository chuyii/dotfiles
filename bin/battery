#!/usr/bin/perl

$capacity = `cat /sys/class/power_supply/BAT0/capacity`;
$status = `cat /sys/class/power_supply/BAT0/status`;

if ($charge == "Charging") {
  print "<fc=#FFFF00>++</fc> ";
} elsif ($capacity <= 10) {
  print "<fc=#FF0000>!!</fc> ";
}

printf "%3d%%\n", $capacity;
